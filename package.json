{
  "name": "opencode-cost-guard",
  "version": "0.1.0",
  "description": "opencode TUI plugin: intercept prompt submission during model peak-pricing windows (e.g. DeepSeek x2) and offer to wait until off-peak",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts",
    "./tui": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsgo --noEmit || tsc --noEmit"
  },
  "keywords": [
    "opencode",
    "tui",
    "plugin",
    "deepseek",
    "pricing",
    "peak"
  ],
  "dependencies": {
    "@opencode-ai/plugin": "^1.18.11",
    "@opentui/core": "^0.5.1",
    "@opentui/solid": "^0.5.1",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0"
  }
}
